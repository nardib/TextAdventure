package org.zssn.escaperoom;

/**
 * Enum for the three different types of lock for an item container
 */
public enum LockType {
    /** key lock */
    KEY,
    /** combination lock */
    COMBINATION,
    /** no lock */
    NONE
}